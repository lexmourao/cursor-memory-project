# Active Context

Current state after the methodology cleanup and runtime compatibility cycle:

- `main` is aligned with the local-first methodology scope.
- OpenAI SDK calls were migrated to the modern client-based API.
- README deployment/archive wording was surgically aligned with the post-cleanup scope.
- Deployment scaffolding and scheduled workflows remain archived as historical reference.
- Root `memory-bank/` remains a starter template.
- This self-applied example lives under `examples/self-applied/memory-bank/`.

Current active work:

- add this concise self-applied memory-bank example;
- keep the change documentation-only and isolated;
- avoid touching root memory templates, code, tests, roadmap, or README in this PR.

Next planned step:

- add a final release note and tag the methodology-applied state.
